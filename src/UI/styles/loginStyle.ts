import { StyleSheet } from "react-native";

const loginStyle = StyleSheet.create({
  container: {
    flex: 1,
    width: "100%",
    padding: "5%",
    alignItems: "center",
  },
});

export default loginStyle;
